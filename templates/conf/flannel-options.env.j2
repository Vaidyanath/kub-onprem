FLANNELD_IFACE={{ ansible_default_ipv4.address }}
FLANNELD_ETCD_ENDPOINTS={% set comma = joiner(',') %}
{% for host in groups['etcd_endpoints'] %}
{{comma()}}http://{{hostvars[host]['ansible_default_ipv4']['address']}}:{{etcd_port}}
{% endfor %}
